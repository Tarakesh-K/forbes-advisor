# 🧪 Testing Strategy

This project uses a combination of **Pytest** for the backend. The goal is to ensure data integrity during ingestion and high availability for the API.

## 🛠️ Running Tests

### 1. Via Docker (Recommended)
The simplest way to run all backend tests in the production environment:
```bash
make test